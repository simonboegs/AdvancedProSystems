import * as React from "React";
//import "bootstrap";
const bootstrap = typeof window !== `undefined` ? require("bootstrap/dist/css/bootstrap.min.css") : null

export function Proclomation(props) {
  return <div class="row"></div>;
}
